C:/Users/vsdia/UtiliWeb/#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,/,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/tsx/dist/tsx/dist/cli.mjs" "$@"
  ret=$?
fi
exit $ret

C:/Users/vsdia/UtiliWeb/#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,/,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/is-docker/is-docker/cli.js" "$@"
  ret=$?
fi
exit $ret

C:/Users/vsdia/UtiliWeb/#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,/,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/acorn/bin/acorn/bin/acorn" "$@"
  ret=$?
fi
exit $ret
